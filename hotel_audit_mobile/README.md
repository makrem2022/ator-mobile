# Hotel Audit Mobile

Base Flutter app for hotel audits with offline-first architecture.

## Structure

- `lib/app/`: app bootstrap, router and providers
- `lib/core/`: domain models and foundational types
- `lib/features/`: feature-first UI screens
- `lib/shared/`: reusable widgets and utilities

## Getting started

```bash
flutter pub get
flutter run
```

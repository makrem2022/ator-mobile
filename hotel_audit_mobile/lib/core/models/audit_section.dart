class AuditSection {
  const AuditSection({
    required this.id,
    required this.auditId,
    required this.title,
    required this.order,
  });

  final String id;
  final String auditId;
  final String title;
  final int order;
}
